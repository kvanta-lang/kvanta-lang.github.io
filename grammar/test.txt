func add(int x, const int y) -> int {
    if (y == 0) {
        return p;
    }
    x = x + 1;
    rectangle(100, 100, x, 100);
    return x + y;
}

global {
    int v = 100;
    const int v2 = 45;
    int p = 300;
    int func1 = 3;
}

func main() {
    int p2 = 3;
    const array<int, 3> nums = {1, 2, 3};
    for i in (1..2) {
        v = 300;
        circle(i,i,10);
        if (i > 0) {
            v = 4;
        }
    }
    circle(add(200, 1, 10), 240, v2);
    some();
}

func some() { 
  circle(320, 240, 100);
  setFigureColor(Color::Red);
  setLineColor(Color::Green);
  circle(320, 240, 50);

  array<int,3> betters = {1, 2, 3};
  betters[1] = 0;


  rectangle(100, 100, 200, 200);

  setFigureColor(Color::Blue);
  rectangle(125, 125, 175, 175);

  for i in (0..10) {
    setFigureColor(Color::Red);
    int a = 50;
    rectangle(0, i * a, (i+1) * a, (i+1) * a);
  }

    const array<int,3> letters = {0, 100, 200};
    letters[2] = 500;
    circle(letters[2], letters[2], letters[1]);
}



